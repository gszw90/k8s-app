# Backend Second App - 所有环境的 K8s 配置
---
# Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: backend-second-app
  labels:
    name: backend-second-app
    environment: shared
    purpose: application-deployment
---
# ConfigMap - 开发环境
apiVersion: v1
kind: ConfigMap
metadata:
  name: second-app-config-develop
  namespace: backend-second-app
data:
  LOG_LEVEL: "debug"
  GIN_MODE: "debug"
  NODE_ENV: "development"
---
# ConfigMap - 预发布环境
apiVersion: v1
kind: ConfigMap
metadata:
  name: second-app-config-staging
  namespace: backend-second-app
data:
  LOG_LEVEL: "info"
  GIN_MODE: "release"
  NODE_ENV: "staging"
---
# ConfigMap - 生产环境
apiVersion: v1
kind: ConfigMap
metadata:
  name: second-app-config-prod
  namespace: backend-second-app
data:
  LOG_LEVEL: "error"
  GIN_MODE: "release"
  NODE_ENV: "production"
---
# Deployment - 开发环境
apiVersion: apps/v1
kind: Deployment
metadata:
  name: second-app-develop
  namespace: backend-second-app
  labels:
    app: second-app
    environment: develop
    version: develop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: second-app
      environment: develop
  template:
    metadata:
      labels:
        app: second-app
        environment: develop
        version: develop
    spec:
      containers:
      - name: second-app
        image: second-app:develop
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 18081
          name: http
        envFrom:
        - configMapRef:
            name: second-app-config-develop
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 5
          periodSeconds: 10
---
# Deployment - 预发布环境
apiVersion: apps/v1
kind: Deployment
metadata:
  name: second-app-staging
  namespace: backend-second-app
  labels:
    app: second-app
    environment: staging
    version: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: second-app
      environment: staging
  template:
    metadata:
      labels:
        app: second-app
        environment: staging
        version: staging
    spec:
      containers:
      - name: second-app
        image: second-app:staging
        imagePullPolicy: Always
        ports:
        - containerPort: 18081
          name: http
        envFrom:
        - configMapRef:
            name: second-app-config-staging
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 5
          periodSeconds: 10
---
# Deployment - 生产环境
apiVersion: apps/v1
kind: Deployment
metadata:
  name: second-app-prod
  namespace: backend-second-app
  labels:
    app: second-app
    environment: prod
    version: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: second-app
      environment: prod
  template:
    metadata:
      labels:
        app: second-app
        environment: prod
        version: prod
    spec:
      containers:
      - name: second-app
        image: second-app:prod
        imagePullPolicy: Always
        ports:
        - containerPort: 18081
          name: http
        envFrom:
        - configMapRef:
            name: second-app-config-prod
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 15
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 10
          periodSeconds: 10
---
# Service - 开发环境
apiVersion: v1
kind: Service
metadata:
  name: second-app-develop
  namespace: backend-second-app
  labels:
    app: second-app
    environment: develop
spec:
  selector:
    app: second-app
    environment: develop
  ports:
  - name: http
    port: 18081
    targetPort: 18081
    protocol: TCP
  type: ClusterIP
---
# Service - 预发布环境
apiVersion: v1
kind: Service
metadata:
  name: second-app-staging
  namespace: backend-second-app
  labels:
    app: second-app
    environment: staging
spec:
  selector:
    app: second-app
    environment: staging
  ports:
  - name: http
    port: 18081
    targetPort: 18081
    protocol: TCP
  type: ClusterIP
---
# Service - 生产环境
apiVersion: v1
kind: Service
metadata:
  name: second-app-prod
  namespace: backend-second-app
  labels:
    app: second-app
    environment: prod
spec:
  selector:
    app: second-app
    environment: prod
  ports:
  - name: http
    port: 18081
    targetPort: 18081
    protocol: TCP
  type: ClusterIP
---
# Ingress - 开发环境
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: second-app-develop
  namespace: backend-second-app
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: "web"
    traefik.ingress.kubernetes.io/router.middlewares: "backend-second-app-headers-develop@kubernetescrd"
spec:
  rules:
  - host: "second-app-develop.localhost"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: second-app-develop
            port:
              number: 18081
---
# Ingress - 预发布环境
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: second-app-staging
  namespace: backend-second-app
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
    traefik.ingress.kubernetes.io/router.middlewares: "backend-second-app-headers-staging@kubernetescrd"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
  - hosts:
    - "second-app-staging.example.com"
    secretName: second-app-staging-tls
  rules:
  - host: "second-app-staging.example.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: second-app-staging
            port:
              number: 18081
---
# Ingress - 生产环境
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: second-app-prod
  namespace: backend-second-app
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
    traefik.ingress.kubernetes.io/router.middlewares: "backend-second-app-headers-prod@kubernetescrd"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
  - hosts:
    - "second-app.example.com"
    secretName: second-app-prod-tls
  rules:
  - host: "second-app.example.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: second-app-prod
            port:
              number: 18081
---
# Middleware - 开发环境
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: headers-develop
  namespace: backend-second-app
spec:
  headers:
    customRequestHeaders:
      X-Environment: "develop"
      X-Forwarded-Proto: "http"
---
# Middleware - 预发布环境
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: headers-staging
  namespace: backend-second-app
spec:
  headers:
    customRequestHeaders:
      X-Environment: "staging"
      X-Forwarded-Proto: "https"
---
# Middleware - 生产环境
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: headers-prod
  namespace: backend-second-app
spec:
  headers:
    customRequestHeaders:
      X-Environment: "production"
      X-Forwarded-Proto: "https"