# Backend Second App Kubernetes 配置文件
# 支持多环境部署：develop, staging, prod
# 使用环境变量进行配置替换

---
# ConfigMap - 环境特定配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-second-app-config
  namespace: ${NAMESPACE}
  labels:
    app: backend-second-app
    environment: ${ENVIRONMENT}
data:
  PORT: "${PORT}"
  LOG_LEVEL: "${LOG_LEVEL}"
  ENVIRONMENT: "${ENVIRONMENT}"
  SERVICE_NAME: "backend-second-app"

---
# Secret - 敏感数据 (生产环境需要加密)
apiVersion: v1
kind: Secret
metadata:
  name: backend-second-app-secret
  namespace: ${NAMESPACE}
  labels:
    app: backend-second-app
    environment: ${ENVIRONMENT}
type: Opaque
data:
  # 示例：base64编码的数据库密码 (请替换为实际值)
  # echo -n "your-password" | base64
  DB_PASSWORD: "${DB_PASSWORD}"
  API_KEY: "${API_KEY}"

---
# Deployment - 应用部署配置
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-second-app
  namespace: ${NAMESPACE}
  labels:
    app: backend-second-app
    environment: ${ENVIRONMENT}
    version: "${IMAGE_TAG}"
spec:
  replicas: ${REPLICAS}
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: backend-second-app
      environment: ${ENVIRONMENT}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: backend-second-app
        environment: ${ENVIRONMENT}
        version: "${IMAGE_TAG}"
    spec:
      containers:
      - name: backend-second-app
        image: ${REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}
        imagePullPolicy: Always
        ports:
        - containerPort: 18081
          name: http
          protocol: TCP
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: backend-second-app-config
              key: PORT
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: backend-second-app-config
              key: LOG_LEVEL
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: backend-second-app-config
              key: ENVIRONMENT
        - name: SERVICE_NAME
          valueFrom:
            configMapKeyRef:
              name: backend-second-app-config
              key: SERVICE_NAME
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: backend-second-app-secret
              key: DB_PASSWORD
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: backend-second-app-secret
              key: API_KEY
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ping
            port: 18081
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always

---
# Service - 服务发现
apiVersion: v1
kind: Service
metadata:
  name: backend-second-app-service
  namespace: ${NAMESPACE}
  labels:
    app: backend-second-app
    environment: ${ENVIRONMENT}
spec:
  selector:
    app: backend-second-app
    environment: ${ENVIRONMENT}
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 18081
  type: ClusterIP

---
# Ingress - 外部访问 (可选)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-second-app-ingress
  namespace: ${NAMESPACE}
  labels:
    app: backend-second-app
    environment: ${ENVIRONMENT}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  rules:
  - host: "${ENVIRONMENT}-backend-second-app.local"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-second-app-service
            port:
              number: 80

---
# ServiceMonitor - 监控配置 (可选，需要 Prometheus Operator)
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: backend-second-app-monitor
#   namespace: ${NAMESPACE}
#   labels:
#     app: backend-second-app
#     environment: ${ENVIRONMENT}
# spec:
#   selector:
#     matchLabels:
#       app: backend-second-app
#       environment: ${ENVIRONMENT}
#   endpoints:
#   - port: http
#     path: /metrics
#     interval: 30s
#     scrapeTimeout: 10s